# Asset Upload

This example demonstrates how to implement a custom asset upload workflow using editor events.

ModuloEditor does not provide a built-in upload implementation.
Instead, you can connect the editor to your own backend, storage provider, or external service.


<ClientOnly>
  <AssetUploadExample />
</ClientOnly>